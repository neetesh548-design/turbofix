name: QRGateway E2E Tests

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'src/pages/QRGateway.jsx'
      - 'src/**'
      - 'tests/**'
      - '.github/workflows/e2e-tests.yml'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'src/pages/QRGateway.jsx'
      - 'src/**'
      - 'tests/**'

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: [18.x, 20.x]
        browser: [chromium, firefox, webkit]

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps ${{ matrix.browser }}

      - name: Run QRGateway tests
        run: npm run test:qr -- --project=${{ matrix.browser }}
        env:
          CI: true

      # Previously the only 2 of 46 Playwright spec files that ever ran in CI
      # were the two above (matched by the "qr-gateway" substring filter).
      # Adding the specs confirmed passing against current app behavior
      # during this audit's follow-up — see progress.md's "e2e test
      # investigation" section for how the rest of the suite was triaged
      # (role-dashboards.spec.js retired as dead-architecture coverage
      # duplicate; most others not yet verified and intentionally left out
      # of CI rather than added unverified).
      - name: Run verified functional/security specs
        run: npx playwright test settings-role-boundary.spec.js dashboard-framework.spec.js lay-owner-dashboard.spec.js --project=${{ matrix.browser }}
        env:
          CI: true

      - name: Upload Playwright Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report-${{ matrix.node-version }}-${{ matrix.browser }}
          path: playwright-report/
          retention-days: 30

      - name: Upload Test Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results-${{ matrix.node-version }}-${{ matrix.browser }}
          path: test-results.json
          retention-days: 30

  mobile-tests:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium webkit

      - name: Run mobile tests
        run: npm run test:qr:mobile
        env:
          CI: true

      - name: Upload Mobile Test Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report-mobile
          path: playwright-report/
          retention-days: 30

  test-summary:
    name: Test Summary
    runs-on: ubuntu-latest
    needs: [test, mobile-tests]
    if: always()
    steps:
      - name: Check Test Results
        run: |
          if [ "${{ needs.test.result }}" = "failure" ] || [ "${{ needs.mobile-tests.result }}" = "failure" ]; then
            echo "❌ Some tests failed"
            exit 1
          else
            echo "✅ All tests passed"
          fi
